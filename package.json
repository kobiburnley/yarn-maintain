{
  "name": "yarn-maintain-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "isbt start",
    "build": "isbt build",
    "bundle": "yarn workspaces run isbt bundle",
    "lint": "yarn workspaces run isbt lint . --passWithNoTests",
    "test": "yarn workspaces run jest --passWithNoTests",
    "release:no-git": "yarn release --force-publish",
    "release:rc": "yarn release:no-git --canary --preid rc --dist-tag rc",
    "release": "lerna publish --message \"Publish [skip ci]\" --exact --conventional-commits"
  },
  "devDependencies": {
    "@isbt/core": "^1.17.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,ts,tsx,json}": [
      "prettier --write"
    ]
  },
  "version": "0.0.0",
  "packageManager": "yarn@3.1.1"
}
